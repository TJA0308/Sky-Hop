{
  "name": "sky-hop",
  "version": "1.0.0",
  "description": "Sky Hop — a sunset sky-island pixel platformer demo",
  "type": "module",
  "scripts": {
    "dev": "node scripts/generate-levels.mjs && vite",
    "build": "node scripts/generate-levels.mjs && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
